{
  "name": "agents_hub",
  "description": "Interact with the AG3NTS hub.",
  "actions": {
    "download": {
      "description": "Fetch a file from the hub, API token will automatically injected",
      "parameters": {
        "type": "object",
        "properties": {
          "filename": { "type": "string", "description": "Name of the file to download (e.g. \"people.csv\") being the last part of an url" }
        },
        "required": ["filename"],
        "additionalProperties": false
      }
    },
    "verify": {
      "description": "Submit an answer for verification",
      "parameters": {
        "type": "object",
        "properties": {
          "task": { "type": "string", "description": "The task name to verify against (e.g. \"people\")" },
          "answer_file": { "type": "string", "description": "Absolute path to the JSON file containing the answer data" }
        },
        "required": ["task", "answer_file"],
        "additionalProperties": false
      }
    },
    "api_request_body": {
      "description": "POST to /api/* with an inline JSON body",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string", "description": "API path segment after /api/ (e.g. \"location\")" },
          "body_json": { "type": "string", "description": "JSON-encoded string of the request body (will be parsed before sending), skip apiKey field, it's gonna be injected automatically" }
        },
        "required": ["path", "body_json"],
        "additionalProperties": false
      }
    },
    "api_request_file": {
      "description": "POST to /api/* with body loaded from a JSON file",
      "parameters": {
        "type": "object",
        "properties": {
          "path": { "type": "string", "description": "API path segment after /api/ (e.g. \"location\")" },
          "body_file": { "type": "string", "description": "Absolute path to a JSON file for the request body" }
        },
        "required": ["path", "body_file"],
        "additionalProperties": false
      }
    }
  }
}
