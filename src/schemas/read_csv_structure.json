{
  "name": "read_csv_structure",
  "description": "Read the structure of one or more CSV files. Returns column names and row counts. Accepts a single CSV file path or a directory containing CSV files.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Absolute path to a CSV file or a directory containing CSV files"
      }
    },
    "required": ["path"],
    "additionalProperties": false
  }
}
