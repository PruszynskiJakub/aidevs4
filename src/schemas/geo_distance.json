{
  "name": "geo_distance",
  "description": "Compute geographic distances between coordinates. Use when a task involves physical proximity, location matching, or distance calculations.",
  "actions": {
    "find_nearby": {
      "description": "Match two sets of locations in bulk. Reads two JSON files (references + queries) and returns JSON array of matching pairs with distance_km, sorted by distance ascending. Empty array if no pairs within radius. Preferred over looping with distance — prepare the two files first, then match in one call.",
      "parameters": {
        "type": "object",
        "properties": {
          "references_file": { "type": "string", "description": "Absolute path to a JSON array of reference points. Each item must have latitude and longitude (numbers). All other fields are passed through as metadata." },
          "queries_file": { "type": "string", "description": "Absolute path to a JSON array of query points. Each item must have latitude and longitude (numbers). All other fields are passed through as metadata." },
          "radius_km": { "type": "number", "description": "Maximum distance in kilometers. Only pairs within this radius are returned." }
        },
        "required": ["references_file", "queries_file", "radius_km"],
        "additionalProperties": false
      }
    },
    "distance": {
      "description": "Compute haversine distance between a single pair of points. Returns distance in km. For comparing multiple points against multiple references, use find_nearby instead.",
      "parameters": {
        "type": "object",
        "properties": {
          "lat1": { "type": "number", "description": "Latitude of the first point (-90 to 90)" },
          "lon1": { "type": "number", "description": "Longitude of the first point (-180 to 180)" },
          "lat2": { "type": "number", "description": "Latitude of the second point (-90 to 90)" },
          "lon2": { "type": "number", "description": "Longitude of the second point (-180 to 180)" }
        },
        "required": ["lat1", "lon1", "lat2", "lon2"],
        "additionalProperties": false
      }
    }
  }
}
