{
  "name": "bash",
  "description": "Execute a shell command in the session output directory.\n\nUse for: data transformation (jq, awk, sort, cut), system inspection (ls, wc, file), and operations not covered by dedicated tools.\n\nCWD is the session output directory. Use absolute paths for files outside it. Output truncated at 20 KB — for large outputs, redirect to a file and inspect afterward. Returns stdout/stderr with exit code.",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "Shell command to execute. Supports pipes and standard Unix tools."
      }
    },
    "required": ["command"],
    "additionalProperties": false
  }
}
