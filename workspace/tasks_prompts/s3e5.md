Your task is called 'savethem'. You must plan an optimal route for a messenger to reach the city of Skolwin.

You have access to a tool discovery endpoint:
- URL: https://hub.ag3nts.org/api/toolsearch
- Payload: {"apikey": "<key>", "query": "your search query"}

Use it to find all the tools and information you need to complete the task. Use our API key from environment.

Hints:
- The messenger must reach the city of Skolwin
- Maps are always 10x10 grids containing rivers, trees, rocks, etc.
- You have 10 food portions and 10 fuel units
- Every move consumes fuel (unless walking) and food. Each vehicle has its own consumption rates.
- Faster movement burns more fuel, slower movement consumes more food. Balance carefully.
- You can exit your vehicle at any time and continue on foot.
- Toolsearch accepts both natural language queries and keywords
- All tools returned by toolsearch use the same format (apikey + query) and return only the 3 best-matching results per query — they dont return everything, so ask multiple focused questions.
- Reaching the goal tile completes the task and yields a flag.

You can write notes, observations, and intermediate results to files in workspace/sessions/ to keep track of your findings as you go. This helps you organize discovered information (map data, vehicle stats, terrain rules) before planning the route.

Submit your answer to https://hub.ag3nts.org/verify:
{"apikey": "<key>", "task": "savethem", "answer": ["vehicle_name", "direction", "direction", "..."]}
Directions are: up, down, left, right. The first element is your chosen vehicle.
